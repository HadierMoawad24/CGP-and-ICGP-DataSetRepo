The experiment's configuration of training and testing the two proposed methods are implemented using
a development tool (PAYTHON/ASP.NET), 
programming language (PAYTHON), 
and database (SQLite), which run from PC/Laptop. 
